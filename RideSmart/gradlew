#!/usr/bin/env sh
# Lightweight gradle wrapper that defers to system Gradle installation.
if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
else
  echo "Gradle is not installed. Please install Gradle 8.2.1 or later." >&2
  exit 1
fi
